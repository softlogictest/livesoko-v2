{
  "name": "dukalive",
  "version": "2.1.0",
  "description": "DukaLive — Local-first live commerce for social sellers",
  "scripts": {
    "build": "cd frontend && rm -f package-lock.json && npm install && npm run build && cp -r dist ../backend/public",
    "start": "cd backend && npm install && node index.js",
    "dev:backend": "cd backend && node index.js",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "engines": {
    "node": "20.x"
  }
}
