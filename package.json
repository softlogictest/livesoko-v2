{
  "name": "livesoko",
  "version": "2.2.0",
  "description": "LiveSoko — Local-first live commerce for social sellers",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build && rm -rf ../backend/public && cp -r dist ../backend/public",
    "start": "cd backend && node index.js",
    "dev:backend": "cd backend && node index.js",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "engines": {
    "node": "20.x"
  }
}
